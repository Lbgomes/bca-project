module.exports = {
    // other configurations
    modulePathIgnorePatterns: [
        "<rootDir>/src/**/*.styles.ts"
    ],
    // or ignore specific files with a regex pattern
    testPathIgnorePatterns: [
        "<rootDir>/src/**/*.styles.ts"
    ]
};